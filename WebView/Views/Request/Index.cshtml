@model IEnumerable<WebView.Models.RequestModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Host)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RequestText)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Host)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RequestText)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RequestModelID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RequestModelID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RequestModelID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
